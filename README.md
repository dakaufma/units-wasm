GNU units, modified to build for WebAssembly. Links against linenoise instead of readline.

Build with `make units.wasm`

See [units.ninja](https://github.com/dakaufma/units.ninja) for context + demo
